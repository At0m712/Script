using UnityEngine;

public class ModuleInfo : MonoBehaviour
{
    public float tailleZ = 20f;   // De combien on avance vers l'avant
    public float hauteurY = 0f;   // De combien on monte (ou descend)
}